services:
  app:
    build: .
    image: alv-java
    container_name: alv-java
    restart: unless-stopped
    ports:
      - "8769:8769"
    volumes:
      # サンプルログ（読み取り専用）。別ディレクトリを試す場合は ALV_LOG_DIR を指定
      - ${ALV_LOG_DIR:-./samples}:/app/logs/samples:ro
    command: ["--host", "0.0.0.0", "--port", "8769", "--dir", "/app/logs/samples"]
